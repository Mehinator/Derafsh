<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Derafsh.Client.MainPage">

    <ScrollView><VerticalStackLayout Padding="20" Spacing="15">

        <Label
        x:Name="StatusLabel"
        Text="وضعیت: قطع"
        FontSize="Large"
        FontAttributes="Bold"
        HorizontalOptions="Center" />

        <ListView ItemsSource="{Binding Servers}" ItemSelected="OnServerSelected" HeightRequest="400">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" Padding="10">
                            <Image Source="{Binding FlagUrl}" WidthRequest="40" HeightRequest="30" />
                            <Label Text="{Binding Country}" FontSize="Medium" VerticalOptions="Center" />
                            <Label Text="-" FontSize="Medium" VerticalOptions="Center" Margin="5,0"/>
                            <Label Text="{Binding City}" FontSize="Small" VerticalOptions="Center" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button
        x:Name="ConnectButton"
        Text="اتصال"
        FontSize="Large"
        Clicked="OnConnectButtonClicked"
        HorizontalOptions="Fill" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
